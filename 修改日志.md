# 修改日志

记录影响功能、架构或关键业务流程的重要修改。各分类内按时间倒序排列。

## 跨端

### 2026-08-19 09:20｜邮箱验证码注册/登录/找回密码功能

- **修改内容**：
  1. 后端新增 SMTP 邮件配置（QQ 邮箱），通过环境变量 `EMAIL_HOST_USER`/`EMAIL_HOST_PASSWORD` 配置
  2. 后端新增 `EmailCodeService`：6位数字验证码，Redis 存储（TTL 5分钟），60秒防重发，假邮箱后缀（@test.com/@example.com/@fake.com）直接返回固定验证码 999999
  3. 后端新增 4 个接口：`send_code`（发送验证码）、`register_with_code`（验证码注册）、`login_with_code`（验证码免密登录）、`reset_password`（重置密码）
  4. 后端新增 `SendCodeRateThrottle`（同一IP每分钟1次）
  5. Web 端 LoginView 改造为 Tab 切换（密码登录/验证码登录），新增忘记密码链接
  6. Web 端 RegisterView 增加验证码字段
  7. Web 端新增 ForgotPasswordView 找回密码页面
  8. APP 端 LoginView 改造为 van-tabs 切换（密码登录/验证码登录）
  9. APP 端 RegisterView 增加验证码字段
  10. APP 端新增 ForgotPasswordView 找回密码页面
- **修改原因**：用户需要邮箱验证码注册、验证码免密登录和找回密码功能；为后续第三方登录预留扩展（EmailCodeService 与视图解耦，JWT 发放逻辑可复用）
- **影响范围**：后端 `settings/dev.py`、`settings/prod.py`、`apps/accounts/services.py`、`apps/accounts/views.py`、`apps/accounts/serializers.py`、`apps/accounts/throttles.py`；Web `src/api/user.js`、`src/views/LoginView.vue`、`src/views/RegisterView.vue`、`src/views/ForgotPasswordView.vue`（新建）、`src/router/index.js`；APP `src/api/user.js`、`src/views/LoginView.vue`、`src/views/RegisterView.vue`、`src/views/ForgotPasswordView.vue`（新建）、`src/router/index.js`
- **验证结果**：未测试

### 2026-08-18 17:40｜脚本统一迁移至 scripts/ 目录

- **修改内容**：将 `deploy.sh`、`dev-local.ps1`、`server_db_dump.py`、`update_db_dump.py` 从项目根目录迁移到 `scripts/`，修改各脚本路径推算逻辑使其在任意目录运行均不受相对路径影响；同步更新 `scripts/backup.sh`、`scripts/server_backup.sh`、`deploy/test_deploy_script.py` 中的路径引用
- **修改原因**：统一管理脚本，避免根目录文件杂乱；脚本迁移后需自动推算项目根目录
- **影响范围**：`scripts/` 下全部脚本；`deploy/test_deploy_script.py`
- **验证结果**：未测试

### 2026-08-18 17:10｜新增服务端数据库转储脚本

- **修改内容**：新增 `scripts/server_db_dump.py` 和 `scripts/server_backup.sh`，通过 `docker compose exec db mysqldump` 导出 Docker MySQL 数据库结构和数据到 `init_data.sql`，首行加上 `USE icube_db;`，并自动 git commit + push 到远程仓库
- **修改原因**：服务端缺少将运行中数据库导出为 `init_data.sql` 的工具，本地 `update_db_dump.py` 依赖 pymysql 直连 MySQL，服务端 Docker 环境下更适合用 mysqldump
- **影响范围**：新增 `scripts/server_db_dump.py`、`scripts/server_backup.sh`；不影响现有 `scripts/update_db_dump.py` 和 `scripts/backup.sh`
- **验证结果**：未测试

### 2026-08-18 16:35｜公式库 Web 端 8 项问题修复

- **修改内容**：
  1. 上传弹窗遮罩 `align-items: center` → `flex-start`，修复内容超视口时顶部不可达
  2. 上传难度选项 5 档→3 档（基础/进阶/困难），与列表筛选统一
  3. 公式详情弹窗 `.formula-detail` 加 `max-height: 65vh; overflow-y: auto`，支持内容滚动
  4. 分类树叶子节点 `cat.phase` → `cat.name`，顶层节点去掉拼接的"法"字
  5. 卡片底部新增"删除"按钮（仅作者可见），详情弹窗 footer 新增"编辑/删除"按钮，新增 `handleDeleteFormula` 二次确认删除方法
  6. 卡片 footer 在分类前新增"浏览：N次"，排序新增"浏览量升序/降序"选项
  7. 作者筛选前端按 id 去重兜底
  8. 后端 `FormulaSerializer` 的 `create()` 和 `update()` 中 target_state 绑定增加 phase 回退：按 `category` 精确查 `CubeState` 失败时，回退按 `category__phase` 查同阶段状态（如自定义"四向F2L"回退到标准 F2L 完成状态）
- **修改原因**：上传弹窗顶部看不全；难度选项上传与筛选不一致；详情页无法滚动；分类按 phase 显示导致重复且含多余"法"字；自己的公式只能编辑不能删除；卡片无浏览量显示且无浏览量排序；作者筛选可能重复；自定义分类"四向F2L"无关联 CubeState 导致 target_state=None、初始状态推导失败
- **影响范围**：前端 `FormulaEditor.vue`、`FormulaLibrary.vue`；后端 `cube_api/apps/formula/serializers.py`
- **验证结果**：未测试

## 前端-Web

### 2026-08-13 10:32｜路由加载反馈与错误兜底

- **修改内容**：新增全局路由加载进度条、内容区加载遮罩和加载失败错误卡片，支持重新加载及返回首页。
- **修改原因**：路由懒加载期间缺少视觉反馈，等待过程生硬；加载失败时可能出现空白页面。
- **影响范围**：`cube_front` 的应用入口、路由守卫、首页布局、加载状态管理及公共状态组件。
- **验证结果**：4 个路由加载状态测试通过，`npm run build` 构建通过。
- **关联提交**：`f912861`

## 前端-App

### 2026-08-18 17:10｜APP端 10 项功能优化与修复

- **修改内容**：
  1. 公式详情返回位置恢复：`FormulaView.vue` 添加 `onDeactivated`/`onActivated` 钩子保存和恢复滚动位置
  2. 双击导航栏"公式"重置筛选：新建 `useTabReset.js` composable，`App.vue` 监听双击事件触发 `resetTrigger`
  3. 我的公式列表筛选失效修复：后端 `views.py` 的 `my_custom` 方法添加 `filter_queryset` 调用
  4. 计时完成后数据同步+下拉刷新：`TimerView.vue` 在 `createTimerRecord` 成功后调用 `loadRecords(true)`，添加页面级 `van-pull-refresh`
  5. 数据记录类型按钮阴影移除：`TimerView.vue` 的 `.records-filter` 设为 `box-shadow: none`
  6. APP端帖子发表/编辑/删除：新建 `PostEditorView.vue`（Markdown 编辑+图片插入+标签选择），`forum.js` 新增 `createPost`/`updatePost`/`deletePost`/`uploadPostImage`/`getTags`，`router/index.js` 新增 `PostCreate`/`PostEdit` 路由，`ForumView.vue` 导航栏右侧添加发帖按钮，`PostDetailView.vue` 帖子作者可见编辑/删除按钮
  7. 论坛收藏tab+计数即时更新：`ForumView.vue` 新增"收藏"tab 调用 `getCollectedPosts`，`PostDetailView.vue` 修复点赞/收藏计数从 `res.data` 改为直接从 `res` 读取
  8. 帖子详情页下拉刷新：`PostDetailView.vue` 添加 `van-pull-refresh` 包裹内容区
  9. 添加公式时禁止左右滑动：`App.vue` 的 `onTouchEnd` 判断 `van-popup--show` 存在时不触发滑动
  10. 公式卡片查看次数+浏览量排序：`FormulaCard.vue` 底部显示查看次数，`FormulaView.vue` 排序选项新增浏览量
- **修改原因**：用户反馈 10 项体验问题和功能缺失
- **影响范围**：`cube_app` 的 `FormulaView.vue`、`FormulaDetailView.vue`、`FormulaCard.vue`、`TimerView.vue`、`ForumView.vue`、`PostDetailView.vue`、`PostEditorView.vue`（新建）、`App.vue`、`router/index.js`、`api/forum.js`、`composables/useTabReset.js`（新建）、`components/common/ConfirmDialog.vue`；后端 `cube_api/apps/formula/views.py`
- **验证结果**：未测试

### 2026-08-17 21:50｜公式输入框支持光标位置编辑

- **修改内容**：
  1. `NotationKeyboard.vue` 新增 `cursorPos` prop 和 `update:cursorPos` emit，所有操作（插入字符、修饰符、空格、删除、清空）改为在光标位置执行而非末尾追加
  2. `FormulaView.vue` 新增公式输入框去掉 `readonly`，加 `inputmode="none"` 防止系统键盘弹出，加入 `trackNotationCursor`/`onNotationCursorChange` 光标追踪
  3. `FormulaDetailView.vue` 编辑公式输入框同样改造
- **修改原因**：公式输入框为 readonly 不可编辑，用户无法在中间位置插入或修改字符，只能在末尾追加
- **影响范围**：`cube_app` 的 `NotationKeyboard.vue`、`FormulaView.vue`、`FormulaDetailView.vue`
- **验证结果**：未测试

### 2026-08-17 21:15｜OCR 公式识别优化：自适应预处理 + 投影分割 + 字符映射

- **修改内容**：
  1. **自适应预处理**：`ocr-helper.js` 的 `preprocessImage` 新增 `isCleanBinaryImage` 检测，干净二值图（黑字白底截图）跳过对比度/二值化/膨胀，仅放大+白边填充
  2. **白名单优化**：加入括号 `()` 到 `tessedit_char_whitelist`，避免 `(U` 被当成单 blob；加入 `tessedit_ocr_engine_mode: '2'`（LSTM）、`preserve_interword_spaces: '1'`、`user_defined_dpi: '300'`；放大倍数 3x→4x
  3. **投影分割字符级识别**：新增 `segmentAndRecognize` 函数，通过行列投影分割字符，用 PSM 10 单字符模式逐个识别，防止撇号 `'` 与相邻字母粘连
  4. **字符映射修正**：`cleanNotation` 新增映射：`/→'`、`|→'`、`H→'`（撇号误认）、`I→r`（小写 r 误认）、`W→删除`（括号误认）；加入 `'`(U+2019) Unicode 撇号替换
  5. **Token 分割**：长度>2 的 token 按字符逐个拆分，遇到 `'` 或 `2` 与前一字符合并
  6. **调用方更新**：`FormulaView.vue` 和 `FormulaDetailView.vue` 的 `multiPassOCR` 调用传入 `procCanvas` 参数，启用投影分割识别
  7. **测试页同步**：`ocr-test.html` 同步所有优化，新增"投影分割识别"开关
- **修改原因**：OCR 识别撇号 `'` 和括号 `()` 失败——白名单不含括号导致 blob 合并、膨胀导致撇号粘连、Tesseract 把撇号误认为 `H`/`/`/`|`
- **影响范围**：`cube_app` 的 `src/utils/ocr-helper.js`、`public/ocr-test.html`、`src/views/FormulaView.vue`、`src/views/FormulaDetailView.vue`；新增 `Code Wiki/18_OCR公式识别技术.md`
- **验证结果**：Node.js 验证 cleanNotation 逻辑通过，seg 输入 `(U/)rU/R/URU\nI/\n` 清洗后输出 `U' r U' R' U R U r'`，完全匹配目标。未进行运行时测试。

### 2026-08-17 19:30｜边缘滑动修复、统计为0修复、异步上传头像、OCR分离、公式下载、双指缩放

- **修改内容**：
  1. **边缘滑动修复**：`App.vue` 滑动逻辑改为边缘优先，屏幕左右 40px 内触摸可切换大 Tab，van-tabs 内容区中间滑动仍交给 swipeable，解决计时器/论坛页面无法切换大 Tab
  2. **统计为0修复**：`api/user.js` 的 `getProfileApi` URL 去掉尾部斜杠，匹配后端 `SimpleRouter(trailing_slash=False)` 路由
  3. **个人中心增强**：`ProfileView.vue` 新增下滑刷新（van-pull-refresh）、点击昵称/简介跳转编辑资料、头像裁剪确认后异步上传（showLoadingToast → updateProfileApi → 更新本地），删除关注/粉丝统计，新增下载统计
  4. **OCR 与上传分离**：`FormulaView.vue` 上传图片不再自动触发 OCR，公式输入区新增"从图片识别"按钮（拍照/相册 → 全图 OCR → 填入公式）
  5. **ImageCropper 双指缩放**：重写 `ImageCropper.vue`，固定 1:1 裁剪框居中，支持双指缩放（1-5x）和单指拖拽图片，约束裁剪框始终在图片内
  6. **公式下载功能**：新增 `formula-download.js` 工具（localStorage 存储），`FormulaDetailView` 收藏按钮旁新增下载按钮，`FormulaView` 列表长按卡片进入多选模式可批量下载/收藏，范围下拉新增"我的下载"
  7. **FormulaCard 增强**：新增 `multiSelect`/`selected`/`downloaded` props，长按 emit `longpress`，多选时显示选择指示器，已下载显示角标
- **修改原因**：计时器/论坛页面 van-tabs swipeable 拦截了大 Tab 切换；profile API URL 尾部斜杠不匹配导致 404 统计为 0；头像裁剪后需手动点保存体验差；OCR 与图片上传耦合不灵活；用户需要离线查看公式
- **影响范围**：`cube_app` 的 `App`、`ProfileView`、`FormulaView`、`FormulaDetailView`、`FormulaCard`、`ImageCropper`、`api/user.js`，新增 `formula-download.js`；后端 `formula/serializers.py`
- **验证结果**：`npm run build` 构建成功，APK 构建成功。未进行运行时测试。

### 2026-08-17 18:25｜头像上传修复、上传公式功能、滑动过渡、计时区域调小、个人中心跳转

- **修改内容**：
  1. **头像上传修复**：用 `@capacitor/camera` 原生 Camera API 替代 `van-uploader`，新增 `ImageCropper.vue`（1:1 裁剪框拖动定位）和 `image-compress.js`（Canvas 裁剪+压缩输出 webp），点击头像区域即可选择拍照/相册
  2. **上传公式功能**：`FormulaView.vue` 右上角新增"+"按钮，弹窗支持图片选择（拍照/相册/公式库引用）、1:1 裁剪、Tesseract.js OCR 识别公式内容、`NotationKeyboard.vue` 键盘式输入（R L U D F B / r l u d f b / M E S x y z / ' 2）、根据步骤数自动分难度（≤6基础、7-10进阶、11+困难）
  3. **字段命名修改**：`FormulaDetailView.vue` 和 `FormulaCard.vue` 中"记号"改为"公式"
  4. **滑动过渡动画**：`App.vue` 添加 transition 组件实现页面切换滑动过渡，根据 Tab 顺序判断方向；触摸滑动逻辑排除 swipeable tabs 区域，优先内部滑动；`ForumView.vue` 和 `TimerView.vue` 的 van-tabs 添加 swipeable 属性
  5. **计时器区域调小**：`TimerDisplay.vue` 高度从 55vh 改为 40vh，min-height 从 280px 改为 200px，字号相应缩小，减少左右滑动误触
  6. **个人中心跳转**：`ProfileView.vue` 帖子和收藏统计项添加点击事件，跳转到我的帖子/我的收藏
- **修改原因**：头像上传在 Android WebView 中 van-uploader 回调不触发；缺少公式上传入口；页面切换无过渡且内部滑动冲突；计时区域过大易误触；个人中心统计项不可点击。
- **影响范围**：`cube_app` 的 `ProfileView`、`FormulaView`、`FormulaDetailView`、`FormulaCard`、`App`、`ForumView`、`TimerView`、`TimerDisplay`，新增 `ImageCropper`、`NotationKeyboard`、`image-compress.js`
- **验证结果**：`npm run build` 构建成功，APK 构建成功（11.4MB）。未进行运行时测试。

### 2026-08-13 22:40｜公式搜索修复、收藏筛选保留、计时Tab拆分、个人数据入口

- **修改内容**：
  1. 后端 `FormulaViewSet` 的 `select_related('author')` 修正为 `select_related('created_by')`，`search_fields` 的 `author__username` 修正为 `created_by__username`，修复全部公式接口 500 错误和搜索不可用
  2. 后端 `FormulaCollectionViewSet.list` 新增 `search` 查询支持（公式名、记号、作者、分类名），收藏页可搜索
  3. 公式库"我的收藏"页保留分类/难度/排序筛选（移除 `v-if="!collectedOnly"` 和 `buildParams` 提前返回）
  4. 计时器页面拆分为"计时器"和"数据记录"两个 Tab，数据记录从后端加载，支持下拉刷新、筛选、左滑删除、卡片详情
  5. 计时器显示添加 "s" 单位（TimerDisplay 主显示 + 统计面板最快/Ao5）
  6. 个人中心标题从"我的"改为"个人中心"
  7. "我的记录"改为"个人数据"（icon: chart-trending-o），移至"我的收藏"和"编辑资料"之间，点击跳转 `/timer?tab=records`
  8. `/timer-records` 路由改为重定向到 `/timer?tab=records`
- **修改原因**：后端 `select_related` 和 `search_fields` 使用了不存在的 `author` 字段（模型实际为 `created_by`），导致全部公式接口 500；收藏页缺少筛选功能；计时器需要数据记录展示；个人中心标题和入口需要调整。
- **影响范围**：后端 `formula/views.py`；前端 `FormulaView`、`TimerView`、`TimerDisplay`、`ProfileView`、`router/index.js`
- **验证结果**：`npm run build` 构建成功，零报错。未进行运行时测试。

### 2026-08-13 22:15｜公式搜索修复、我的收藏筛选、下拉刷新、计时记录详情、我的记录模块、左右滑动切页

- **修改内容**：
  1. 公式库搜索支持作者和分类关键词（后端 `search_fields` 已含 `author__username`、`category__name`）
  2. 公式库新增"我的收藏"筛选下拉项，选中后只展示已收藏公式，其余筛选重置为默认
  3. 公式库添加下拉刷新（`van-pull-refresh`）
  4. 计时器历史记录点击弹出卡片详情（底部 `van-popup`），展示时间、类型、方法、打乱公式
  5. 个人中心新增"我的记录"入口，跳转到 `TimerRecordsView` 查看后端计时记录
  6. 新增 `/timer-records` 路由（`noTabbar` + `requiresAuth`）
  7. `App.vue` 实现左右滑动切换 Tab 页（阈值 80px、水平 > 1.5x 垂直、排除 swipe-cell）
- **修改原因**：用户反馈搜索无法匹配作者和分类；缺少收藏筛选和下拉刷新；历史记录无法查看打乱公式详情；个人中心无计时记录入口；缺少手势切换页面功能。
- **影响范围**：`cube_app` 的 `FormulaView`、`TimerView`、`TimerRecordsView`、`ProfileView`、`App.vue`、`router/index.js`；后端 `formula/views.py`（`search_fields` 已含作者和分类）
- **验证结果**：`npm run build` 构建成功，零报错。未进行运行时测试。

## 后端

### 2026-08-13 22:40｜公式接口 FieldError 修复 + 收藏搜索支持

- **修改内容**：
  1. `FormulaViewSet.queryset` 的 `select_related('author')` 改为 `select_related('created_by')`
  2. `FormulaViewSet.search_fields` 的 `'author__username'` 改为 `'created_by__username'`
  3. `FormulaCollectionViewSet.list` 新增 `search` 参数支持（`formula__name`、`formula__notation`、`formula__created_by__username`、`formula__category__name`）
- **修改原因**：模型字段为 `created_by` 而非 `author`，导致 `FieldError` 全部公式接口 500；收藏接口缺少搜索功能。
- **影响范围**：`apps/formula/views.py`
- **验证结果**：未进行运行时测试。

## 跨端

暂无记录。

## 工程与部署

暂无记录。
